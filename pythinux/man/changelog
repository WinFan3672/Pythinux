div()
Pythinix 2.4 Changelog
div()
* pkm/installd
    * When installing dependencies, only the required dependencies are installed.
* Users
    * Added custom user groups.
        * These custom groups have customisable permissions with the new `group` command.
        * The `root` and `user` groups are locked by default. Locked groups are unremovable.
    * user list is no longer stored as a list. 
* Sudo command
    * Allows for running root commands as a non-root user.
div()